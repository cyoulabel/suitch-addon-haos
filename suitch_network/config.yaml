name: "Suitch Network"
description: "Integración con suitch.network — publica sensores vía MQTT discovery"
version: "1.6.2"
slug: "suitch_network"
init: false
arch:
  - aarch64
  - amd64

homeassistant_api: true

options:
  email: ""
  password: ""
  scan_interval: 60
  id_token: ""
  insecure_ssl: false
  mqtt_host: "core-mosquitto"
  mqtt_port: 1883
  mqtt_user: ""
  mqtt_password: ""

schema:
  email: str
  password: str
  scan_interval: int
  id_token: str
  insecure_ssl: bool
  mqtt_host: str
  mqtt_port: int
  mqtt_user: str
  mqtt_password: str
